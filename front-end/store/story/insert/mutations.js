export const updateTags = (state, { tags }) => {
  state.tags = tags;
};

export const updateSelectedTags = (state, { selectedTags }) => {
  state.selectedTags = selectedTags;
};

export const updateImages = (state, { images }) => {
  state.images = images;
};
